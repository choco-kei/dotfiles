#! /bin/sh

#CURDIR=$(cd $(dirname $0) && pwd)
#echo $CURDIR

TARGET=`pwd`
#cho $TARGET
ctags --append=yes  --languages=php  --exclude=vagrant --regex-php="/^[ \t]*const[ \t]+([[:alnum:]_]+)/\1/d/" -f $TARGET/.tags $TARGET/
